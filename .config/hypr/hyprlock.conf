source = ~/.config/hypr/hypr-colors.conf

# Change if wanting a sep. wallpaper for lockscreen
$wallpaper_lock = $wallpaper

# BACKGROUND
background {
    monitor =
    path = $wallpaper_lock
    blur_passes = 2
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

# GENERAL
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = false
    hide_cursor = true
}


# Hour-Time
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%I")"
    # color = rgba(108, 156, 240, .6)
    color = $color6
    font_size = 180
    # font_family = AlfaSlabOne 
    font_family = Mononoki Nerd Font
    position = 0, 300
    halign = center
    valign = center
}

# Minute-Time
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%M")"
    # color = $background
    color = $color8
    # rgba(255, 255, 255, .6)
    font_size = 180
    # font_family = AlfaSlabOne
    font_family = Mononoki Nerd Font
    position = 0, 75
    # position = 0, 100
    halign = center
    valign = center
}

# This is the AM/PM thing
# label {
#     monitor =
#     text = cmd[update:1000] echo -e "$(date +"%p")"
#     color = rgba(255, 255, 255, .6)
#     font_size = 30
#     # font_family = AlfaSlabOne
#     font_family = $lockscreen_font_family
#     position = 170, 0
#     # position = 0, -30
#     halign = center
#     valign = center
# }


# Day-Date-Month
label {
    monitor =
    text = cmd[update:1000] echo "<span color='##ffffff99'>$(date '+%A, ')</span><span color='##6c9cf0'>$(date '+%d %B')</span>"
    font_size = 30
    # font_family = SF Pro Display Bold
    font_family = Mononoki Nerd Font Bold
    # font_family = $lockscreen_font_family
    # position = 0, -80
    position = 0, -80
    halign = center
    valign = center
}

# USER
label {
    monitor =
    text = 
    color = rgba(255, 255, 255, .65)
    font_size = 100
    position = 0, -180
    halign = center
    valign = center
}


# INPUT FIELD
input-field {
    monitor =
    size = 250, 60
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(0, 0, 0, 0)
    inner_color = $foreground
    font_color = rgb(200, 200, 200)
    fade_on_empty = false
    # font_family = SF Pro Display Bold
    font_family = FiraCode Nerd Font Semi-Bold
    placeholder_text = <i><span foreground="##ffffff99">Hi, $USER</span></i>
    hide_input = false
    position = 0, -290
    halign = center
    valign = center
}



## Testing to see if I can get an image to be visible?
# image {
#     monitor = 
#     # text = cmd[update:1000] echo "$(~/dotfiles/.config/get_spotify_info.fish oo)"
#     path = ~/Pictures/wallpaper_locks/lofi_cat.png
#     size = 250
#
#     font_size = 18
#     # font_family = JetBrains Mono Nerd, SF Pro Display Bold
#     # font_family = Mononoki Nerd Font Bold
#     rounding = 0 # Negatives result in rounded corners etc. eventually a circle
#     reload_time = 600 # 10 Minutes
#     reload_cmd = ""
#     position = 0, -475
#     halign = center
#     valign = center
#     zindex = 0
# }

# image {
#     monitor = 
#     # text = cmd[update:1000] echo "$(~/dotfiles/.config/get_spotify_info.fish oo)"
#     # text = cmd[update:1000] echo "$(~/dotfiles/.config/get_spotify_info.fish)"
#     size = 120
#     # font_size = 18
#     # font_family = JetBrains Mono Nerd, SF Pro Display Bold
#     # font_family = Mononoki Nerd Font Bold
#     rounding = 10 # Negatives result in rounded corners etc. eventually a circle
#     reload_time = 3 # 600 = 10 Minutes, 3 = 3 seconds
#     reload_cmd = ~/dotfiles/.config/get_spotify_info.fish
#     position = 0, -475
#     halign = center
#     valign = center
#     # zindex = 0
# }


# CURRENT SONG
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/songdetail.sh)" 
    color = rgba(255, 255, 255, 0.7)
    font_size = 18
    # font_family = JetBrains Mono Nerd, SF Pro Display Bold
    font_family = Mononoki Nerd Font Bold
    # font_family = $lockscreen_font_family, Bold
    position = 0, 60
    halign = center
    valign = bottom
}
