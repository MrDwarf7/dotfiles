#########################
$hyprdir = $HOME/.config/hypr
# $keymaps_dir = $hyprdir/configs/keymaps
#########################

# TODO: check syntax lol
#
# layerrule = noanim ^(dms)$

# ## Blur Tips
# -- For whatever reason.... this is in the Useful Utilities :: Status Bars
# part of the wiki.... God knows why
#
# Use the blur and ignore_alpha layer rules.
# The former enables blur, and the latter makes it ignore insufficiently opaque regions.
# Ideally, the value used with ignore_alpha is higher than the shadow opacity and lower than the bar/menu content’s opacity.
# Additionally, if it has transparent popups, you can use the blur_popups rule.




layerrule {
    name = "dms-no-anim"
    no_anim = on
    match:namespace = dms
}

layerrule {
    name = "dms-control-center-animation"
    animation = slide right
    match:namespace = dms:control-center
}

layerrule {
    name = "dms-dash-animation"
    match:namespace = dms:dash
    animation = slide left
}

layerrule {
    name = "dms-workspace-overview-animation"
    animation = slide top
    match:namespace = dms:workspace-overview
}


# O => modal
# m => misc
# p => popout

# ### Modals = o
# Clipboard history           |     dms:clipboard
# File browser                |     dms:file-browser
# Settings                    |     dms:settings
# Launcher                    |     dms:spotlight
# Bluetooth pairing           |     dms:bluetooth-pairing
# Color picker                |     dms:color-picker
# Hyprkeybinds                |     dms:hyprkeybinds
# Network info                |     dms:network-info
# Network info (wired)        |     dms:network-info-wired
# Notification                |     dms:notification-center-modal
# Polkit                      |     dms:polkit
# Power menu                  |     dms:power-menu
# Process list                |     dms:process-list-modal
# Wifi password               |     dms:wifi-password
# Confirm modal               |     dms:confirm-modal
# Fallback namespace          |     dms:modal


# ### Popouts = p
# App drawer                  |     dms:app-launcher
# Control center              |     dms:control-center
# Battery                     |     dms:battery
# Vpn                         |     dms:vpn
# DankDash                    |     dms:dash
# Notification center         |     dms:notification-center-popout
# Process list                |     dms:process-list-popout
# Fallback namespace          |     dms:popout
# Plugin                      |     dms:plugins:<namespace>
# Fallback plugin namespace   |     dms:plugins:plugin

# ### Misc components = m
# DankBar                     |     dms:bar
# Dock                        |     dms:dock
# Workspace Overview          |     dms:workspace-overview
# Notification Popup          |     dms:notification-popout
# OSD                         |     dms:osd
# Slideout                    |     dms:slideout
# Tooltip                     |     dms:tooltip
# Dock context menu           |     dock-context-menu # ####### NOTE!!!!!!!! Not a typo !!!!!!!!! not in the `dms:` namepsace!
# Toast                       |     dms:toast
# Tray menu window            |     dms:tray-menu-window


#Make transparent
layerrule { 
    name = "dms-blur-for-popouts-and-misc"
    blur = on
    # blur_popups = false
    ignore_alpha = 0
    # ignore_alpha = 0.3
    xray = false
    match:namespace = dms:(bar|tooltip|toast|dock-context-menu|control-center|notification-center-popout|dash|battery|popout|app-launcher)
#                   #      m     m       m       m               p               p                        p     p        p        p
}


#Make solid -- "" blurred "" (not really), but let it be see-through
layerrule { 
    name = "dms-blur-for-modals-and-misc"
    blur = on
    # blur_popups = false
    ignore_alpha = 0
    # ignore_alpha = 0.2
    xray = false
    match:namespace = dms:(polkit|notification-center-modal|notification-popup|color-picker|clipboard|clipboard-popout|spotlight|settings|tray-menu-window|slideout|system-update|system-update:background|filebrowser|osd)  
#                   #        o       o                       ??                    o          o         o         o         m              m           ??             ??                   ??          m
}

layerrule { 
    name = "dms-blur-for-process-list"
    blur = on
    # ignore_alpha = 0.2
    ignore_alpha = 0
    xray = false
    match:namespace = dms:(process-list-modal|process-list-popout)
}

# layerrule { 
#     name = "dms-blur-for-TESTING" ## The right click menu thing
#     blur = on
#     ignore_alpha = 0
#
#     # ignore_alpha = 0.6
#     # ignore_alpha = 0.5
#     # ignore_alpha = 0.4
#     xray = true
#     # process-list-popout ## the main actual window thing
#     # match:namespace = dms:(process-list-modal|process-list-modal:background|process-list-popout|process-list-popout:background)
# }


# For blur/alpha changes - when we go from 0.274 => 0.275
# Is when we get the 'entirely see through' problem as somewhere is calling a math.max(...) fn'
# blur on, ignore_alpha 0.274,


####### minor fixes to make solid again, will be moved into the make solid above
# layerrule = blur on, ignore_alpha 0.274, match:namespace dms:(tray-menu-window|slideout)
