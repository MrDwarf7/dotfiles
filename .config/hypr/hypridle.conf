# source /home/$USER/.cache/wal/colors-hyprland
# source = ./common_vars.conf

general {
    inhibit_sleep = 2
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on # to avoid having to press a key twice to turn on display
    ignore_dbus_inhibit = /opt/spotify/spotify
}

# timeout = 600     # 10 minutes
# timeout = 900     # 15 minutes

##  test listener
# listener {
#     timeout = 40
#     on-timeout = notify-send "40 second mark"
#     on-resume = notify-send "40 second mark resumed from"
# }


listener {
    timeout = 600                                # 10 minutes
    on-timeout = brightnessctl -s set 10         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # monitor backlight restore.
}

listener { 
    timeout = 650                                                     # 10 minutes and 30 seconds
    on-timeout = brightnessctl -sd rgb:kbd_backlight set 0            # turn off keyboard backlight.
    on-resume = brightnessctl -rd rgb:kbd_backlight                   # turn on keyboard backlight.
}

listener {
    timeout = 1200                                                     # 20 minutes
    on-timeout = loginctl lock-session                                # lock screen when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # && notify-send "System" "Keyboard backlight turned off after 15 minutes of inactivity." -i $wallpaper
}

listener {
    timeout = 1250                                                     # 15 minutes and 30 seconds
    on-timeout = hyprctl dispatch dpms off                            # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 1800                                # 30min
    on-timeout = systemctl suspend                # suspend pc
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # turn on screen and restore brightness etc.
}
