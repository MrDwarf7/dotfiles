# vim: set ft=ghostty syntax=conf:
# Loaded via config-file = ?os.conf (symlink to this file on Darwin).
# window-decoration left unset so Ghostty uses the macOS default titlebar.

background-opacity = 0.95
# window-decoration = none
font-size = 14.0
